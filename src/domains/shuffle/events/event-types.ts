export type ShuffleWinnerSelected = {
  type: 'shuffle-winner-selected';
  source: 'song-queue';
};

export type UserEnteredInShuffle = {
  type: 'user-entered-in-shuffle';
  source: 'song-queue';
};
